import _ from 'lodash';
import React from 'react';
import PropTypes from 'prop-types';

// The cluster screen
const Cluster = () => (
  <div>
    Success
  </div>
);

Cluster.propTypes = {
};

export default Cluster;
