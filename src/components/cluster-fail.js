import _ from 'lodash';
import React from 'react';
import PropTypes from 'prop-types';

const ClusterFail = () => (
  <div>
    Failed
  </div>
);

ClusterFail.propTypes = {
};

export default ClusterFail;
